const { FILE_STATUS } = require("./fileConst");
const { FILE_MESSAGES } = require("./messages");
const { RESPONSE_CODE, RESPONSE_STS_CODE } = require("./responseConst");

module.exports = {
  RESPONSE_CODE,
  RESPONSE_STS_CODE,
  FILE_STATUS,
  FILE_MESSAGES
};
